// Barrel for the app-only component layer (primitives, states — see docs/adr/0007).
// Populated by slice P2 (primitives), S2 (shell), and S10 (edge states).
export * from "./primitives/index.js";
export * from "./shell/index.js";
