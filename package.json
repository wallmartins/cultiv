{
  "name": "cultiv",
  "version": "0.1.0",
  "private": true,
  "description": "Cultiv monorepo — marketing surface (apps/web) and shared UI (packages/ui)",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @my-ai-orchestrator/web build",
    "dev:web": "pnpm --filter @my-ai-orchestrator/web dev",
    "lint": "pnpm -r --if-present lint"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
