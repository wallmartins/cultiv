# Origin of the authenticated product app (Auth0 login + /app/* routes).
# Bare https origin only — no path, query, or trailing slash. A malformed
# value fails `astro build` (validated in src/config.ts).
# Defaults to https://cultiv.app when unset.
PUBLIC_APP_ORIGIN=
