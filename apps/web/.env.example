# Public site URL for canonical and hreflang tags (no trailing slash).
SITE_URL=https://cultiv.app

# Loops waitlist (server-only — set in Vercel Project Settings, never expose to the client).
LOOPS_API_KEY=
LOOPS_MAILING_LIST_ID=
